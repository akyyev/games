window.CHECKERS_CONFIG = {
  // For local server testing use: "ws://localhost:10000"
  // For Render use your deployed service URL, for example:
  // "wss://your-checkers-server.onrender.com"
  WS_URL: "wss://checkers-app-hdoh.onrender.com",
  // WS_URL: "ws://localhost:10000",
};
